// Entities, Zod schemas and pure calculation; owned by the contracts scope.
// "export {}" is what makes this file a module: under isolatedModules and
// verbatimModuleSyntax an empty file is a script, and a script cannot be
// imported, so the workspace graph would not resolve without it.
export {};
